<nav class="navbar navbar-expand-md navbar-dark">
 <%= link_to image_tag("/images/notflix.png"), class: "navbar-brand ml-3 mt-2"%>

  <button class="navbar-toggler navbar-dark" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

      <li class="nav-item active">
        <%=link_to "Movies", movies_path, class: "nav-link text-white font-nf"%>
      </li>

      <li class="nav-item">
      <%=link_to "Genres", tags_path, class: "nav-link text-white font-nf"%>
      </li>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-white font-nf" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Welcome <%=@current_user.username.titleize %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
        <%= link_to "Account Homepage", user_path(@current_user), class: "dropdown-item font-nf" %>
        <%= link_to "Account Settings", edit_user_path(@current_user), class: "dropdown-item font-nf" %>
          <div class="dropdown-divider"></div>
          <%= link_to "Logout", logout_path, class: "dropdown-item font-nf" %>
        </div>
      </li>

    </ul>
  </div>

</nav>